# NATS server config for the local dev stack.
# no_tls enables the plain ws:// transport the browser chat client (nats.ws)
# uses on port 8081. Local development only — production must use TLS.
websocket {
  port: 8081
  no_tls: true
}
